---
"@weaver/config-engine": minor
---

Restructure JSON Schema output from flat `x-weaver-*` keys to a single `x-weaver` object and emit all extension fields (sensitive, maxOverrideLayer, writeRestriction, sessionMode, expressionAllowed, instanceOverridable, viewConfig)
